import React from 'react';
import * as Styled from './style';

const SeletorDePersonagem = () => {

    return (
        <Styled.Container>
            <Styled.Title>Heroes!</Styled.Title>
        </Styled.Container>
    )
}

export default SeletorDePersonagem;
